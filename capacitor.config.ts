import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'myAppConfiteria',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
